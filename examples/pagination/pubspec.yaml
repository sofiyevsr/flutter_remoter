name: pagination
description: A simple pagination example for InfiniteRemoterQuery

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  remoter:
    path: "../../packages/remoter"
  flutter_remoter:
    path: "../../packages/flutter_remoter"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
